Fixes applied to pass the Day 7 code review checklist:

1. Added try/except handling around every pipeline stage. Each handler logs the stage name and row counts collected before re-raising the error.
2. Removed hardcoded runtime paths. All input, output, Gold table, metadata, and run-date values now come from command-line parameters.
3. Added row count logging after input, cast/filter/dedup, and output transformations. Counts are also written into run metadata.
4. Added schema validation before Bronze, Silver, merchant, and Gold transformations.
5. Added NULL checks for transaction_id, merchant_id, customer_id, transaction_date, status, and amount.
6. Preserved the existing pass items: overwrite writes, partition pruning, COMPLETED-only revenue, broadcast merchant join, transaction_id deduplication, run metadata output, and specific imports.

Day 7 completed and validated.
